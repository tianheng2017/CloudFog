# 2026-09-06 CloudFog B1 实施日志

## 阶段 2 s2-1 交付：迁移基础设施（已完成并审查修复）

- **交付物**：
  - `backend/migrations/embed.go`：`//go:embed *` 把迁移 SQL 内嵌进单二进制
  - `backend/internal/migrate/migrate.go`：golang-migrate **v4.19.1** 以 library 集成（iofs 源 + postgres 驱动）；API：`Up`（ErrNoChange 视为成功=幂等）、`Down N`（只显式回滚）、`Status`、`RequireSchemaVersion`（01 §10 启动迁移检查）；DSN 必须 `postgres://` 前缀
  - `backend/cmd/cloudfog/main.go`：单二进制骨架，`migrate up|down N|status` + `--role` 占位（阶段 3）；`config.LoadDotEnv` 对缺失 .env 宽容（不影响 version 等子命令）
  - 单测：`migrationFiles` 解析（排序/去重/忽略 embed.go 与裸 .sql/无下划线 panic 防护/版本冲突报错），fstest.MapFS 驱动
- **端到端验证**：真实 `postgres:18-alpine` 容器 healthy 后 `cloudfog migrate status` 连库 exit 0
- **审查修复记录**：① 解析逻辑抽出成可测函数并补 3 测试；② gofmt 收敛全部 s2-1 文件；③ **02 §11/§15.6 命名示例补 `.up.sql`**（golang-migrate 只认 `<ver>_<desc>.up/down.sql`，裸 .sql 静默忽略）
- **PG18 镜像陷阱**（本轮发现已修）：挂载须用父目录 `/var/lib/postgresql`（数据落 `18/docker` 子目录）；挂旧路径 `/var/lib/postgresql/data` 判定 unused 拒绝启动。dev compose 与 10 §2 均已改
- **既有 gofmt 债务已全部清理**（2026-09-06 下午）：internal/config 4 个文件 + internal/model/db.go + internal/pkg/logger 2 文件，现全库 `gofmt -l` 干净（logger.go 的注释 `//（`→`// （` 是 gofmt 注释规范，已收敛）
- **s2-1 第二轮审查**：移除 `RequireSchemaVersion` 从不使用的 `ctx` 参数（golang-migrate v4 无 ctx API，签名误导）→ 签名 `RequireSchemaVersion(dsn string, want uint64)`。终验 BUILD/VET/TEST/gofmt 全绿。审查通过，无已知缺陷

## s2-2 完成（22 核心模型，2026-09-06 傍晚）

internal/model 新增 21 个模型文件（+common.go/types.go 共 23），对齐 02 §15.1 文件清单与 §15.4 权威示例逐字段转录：user/user_balance/api_key/group/user_allowed_group/provider/channel/channel_group/proxy/model/model_price/model_mapping/usage_log/usage_daily_stat/billing_ledger/subscription_plan/subscription/payment_provider/payment_order/idempotency_record/audit_log/setting。纪律落地：Decimal 直通 numeric、timestamptz、SoftDelete 只用于业务实体、source/source_id（02 §16）、索引 tag 与迁移 SQL 将建的 idx_* 同名（usage_log 的 created_at 复合 priority:2 等）、部分唯一/部分索引只在迁移 SQL。decimal 升 v1.4.0。
**范围澄清**：internal/model 只含 02 §15.1 的 22 核心表；5 张 ops 支撑表（announcements/announcement_reads/tickets/ticket_messages/migration_progress，02 §16/13-operations）随 ops 模块实现时建模与迁移（非丢弃）。BUILD/VET/TEST/gofmt 全绿。

## s2-3 完成（migrations/0001_init，真实 PG 全门禁通过）

- `20260906000001_init.up.sql`（489 行）/ `down.sql`（26 行）：22 核心表全量 DDL。要点：
  - **usage_logs** 按月分区（PK 含分区键 (id,created_at)，identity id，13 个月分区 202608~202708；索引名与模型 tag 精确同名 idx_usage_logs_*；`usage_logs_guard()` BEFORE UPDATE/DELETE 触发器实现只追加（传播到分区）；后续月分区由 ops 预建）
  - **部分唯一索引**：uq_users_email_active/username（WHERE deleted_at IS NULL）、uq_ledger_settle/refund（幂等最终防线）、uq_subscription_period、uq_payment_orders_trade_no/refund_no（WHERE <>''）、uq_groups_name_active、uq_models_name_active
  - **usage_daily_stats 用 PG15+ `UNIQUE ... NULLS NOT DISTINCT`** 解决空值占位唯一语义
  - **外键**：关联表 CASCADE、业务实体 RESTRICT（软删），usage_logs 分区大表不加 FK
  - down 用 DROP TABLE ... CASCADE（分区随父删）+ 触发器/函数清理
- **真实 PG18 验证**：up → 23 core 表（含 schema_migrations）/13 分区/36 总表 → 功能抽查（分区路由 usage_logs_202609、append-only 触发 ERROR、FK 拦截、settle 唯一 uq_ledger_settle duplicate key）→ **down 1 剩 1 表 → up 再应用 36 表**，幂等门禁（12 §5.1）通过
- 遗留：psql 不在本机 PATH（用 docker exec cloudfog-postgres psql）；s2-4 internal/task（AMQP 拓扑 + TaskEnqueuer + consumer + 重试桶/DLQ）进行中

**s2-3 审查修复（3+1 处）**：
- 补 doc §3.1：`uq_users_phone_active`（phone 部分唯一 WHERE deleted_at IS NULL AND phone IS NOT NULL）
- 补 doc §4.2：`idx_channels_deleted`（deleted_at 部分索引）
- 补 doc §6.1：usage_logs `status_code >= 400` 部分索引逐分区 13 个（**分区父表不允许部分索引**）
- **重要教训：PG 索引名 schema 级全局唯一**——逐分区建同义索引必须带分区后缀命名（idx_usage_logs_YYYYMM_status_code），否则第 2 个即 "already exists"。月分区维护脚本必须沿用此命名规则
- dirty 恢复操作：golang-migrate 失败会写 dirty 版本；本地用 `DELETE FROM schema_migrations` 重置后重跑（CLI 未暴露 force）
- 复验：phone/channels/13 status_code 索引全落地；down1→up 幂等门禁再通过；36 表 applied 干净
- **s2-3 二轮审查补充事实**：父表索引传播到分区时 PG **自动重命名克隆**（usage_logs_202609_request_id_idx 等分区名+列名形态），与手工逐分区建索引（须自己保证全局唯一名）机制不同；每分区 9 索引（8 克隆+1 手工 partial）+ pkey，13 分区索引数一致（distinct cnt=1）。无新缺陷

## s2-4 完成（internal/task，真实 RabbitMQ E2E 通过）

- `task.go`：TaskType/QueueName 常量、Task 契约（Type/Queue/Payload json.RawMessage/Key/MaxRetry/Timeout，01 §5.3）、Marshal/UnmarshalTask、**Registry**（任务↔队列绑定，Bind 防改绑；拓扑声明与 Enqueue 校验共用；DefaultRegistry 种子绑定 14 个任务类型）
- `topology.go`：EnsureTopology 幂等声明——main direct `cloudfog.tasks` + DLX fanout + task.dlq；业务三队列 durable 带 x-dead-letter-exchange；**重试/延迟 fanout ingress 桶**（cloudfog.retry.<档> / cloudfog.delay.<档> + retry.<档>/delay.<档> 队列，x-message-ttl + DLX 回主交换）——fanout 忽略 rk 但消息保留，TTL 死信按任务类型回原队列（01 §7.1 反例规避）
- `enqueuer.go`：TaskEnqueuer 接口 + RabbitEnqueuer（durable mode2 + confirm(DeferredConfirm.Wait bool) + mandatory + NotifyReturn 上报无路由 + 单次自动重连重试 + 优雅 Close）；EnqueueIn 取 ≥delay 最小档位、超最大档报错；MemoryEnqueuer 供单测
- `consumer.go`：RunConsumers 每并发独立 channel（Qos prefetch，禁共享），同步处理；失败按 attempt 选 retry 桶 → 桶投递确认成功后再 Ack 原消息（01 §7.3），投递失败 Nack requeue；attempt≥MaxRetry → Nack(requeue=false)→DLQ；无 handler/坏负载→DLQ；优雅关闭等 in-flight drain（shutdown_timeout）
- **单测**：formatBucket/pickBucket/retryBucket/Task round-trip/Registry（7 用例）
- **E2E 冒烟**（smoke_test.go，//go:build integration + CLOUDFOG_RABBITMQ_URL）：rabbitmq:4.2-management 容器 healthy；**Enqueue 即时路由 ✓；EnqueueIn delay.1s → 立即消费拿不到 → 1s 后 TTL 死信回 default 队列（rk 原样保留）✓**
- **API 陷阱记录（amqp091-go v1.12）**：无 DialContext（需自包 dial）；DeferredConfirmation.Wait() 返回 bool（非 error，!ok=未确认）；header 回传值为 string 非 []byte
- 全模块 BUILD/VET/TEST/gofmt 绿；dev 已起 postgres+rabbitmq 容器（cloudfog/cloudfog@cloudfog）
- 遗留：run 主入口/角色启动（阶段 3）；consumer 前缀与配置 wiring；Dockerfile/CI testcontainers 加 rabbit；Rabbit 降级 WAL（阶段 3 需落地）；s2-5 自审

**s2-4 审查修复（5 处）**：① consumer channel 补 `ch.Confirm(false)`——此前未开 confirm，重试桶重发的 DeferredConfirm 返回 nil 当成功，broker 侧可能丢失（01 §7.3「先确认重发成功再 ack」形同虚设）；② 在途任务 ctx 与优雅关闭解耦——改从 `context.Background()` 派生超时（父 ctx 取消不再中断在途，杜绝关闭时任务被取消→requeue 循环）；③ msgs 通道关闭时打日志留痕（重连属 supervisor 层职责，阶段 3）；④ RabbitEnqueuer publish 持锁覆盖 publish+confirm 等待（channel 非线程安全且防 Wait 期间被 reconnect 替换 channel）；⑤ 防护：enabled 组 MaxRetry/Prefetch 必须为正（Prefetch≤0 → Qos 无限预取）、Timeout≤0 兜底 30s（WithTimeout(0) 立即取消）。E2E 回归通过

**s2-4 二轮审查修复（2 处）**：⑥ **主交换机名贯穿修正**——`EnsureTopology` 新增 mainExchange 参数（发布、业务 binding、重试/延迟 TTL 桶 DLX 全部指向同一交换机；此前 declareTTLBuckets 硬编码常量 cloudfog.tasks，config 自定义 exchange 时 TTL 死信会发回错位交换机 → broker 静默丢弃，资金级隐患）；⑦ `formatBucket` 改秒粒度精确（90s 不再截断成 1m），新增 90s/2h 回归用例。E2E 回归通过（1.07s）

**s2-4 三轮审查修复（3 处，自包含/健壮性）**：⑧ `RunConsumers` 启动前自包含 `EnsureTopology`（broker 卷清空/重建后不依赖外部预热，拓扑失败即整体失败而非 worker 静默退出）；⑨ **无启用消费者组时立即返回 nil**（此前 `<-ctx.Done()` 无限空转阻塞；MVP low 停用场景正确落地，新增 TestRunConsumersNoneEnabled）；⑩ Registry/Exchange nil/空兜底（默认 DefaultRegistry/DefaultExchange）。全量回归 BUILD/VET/TEST/E2E 绿

**s2-4 四轮审查（覆盖空白清零 + 1 修复）**：⑪ formatBucket 补亚秒精度（500ms→"500ms"，此前 sub-second 截断成 "0s"）；**⑫ 首次跑通 consumer 全链路 E2E（TestRabbitConsumerRetryDLQ）**——此前 E2E 只测 Enqueuer，RunConsumers→handler 失败→1s 重试桶(attempt=1)→再失败(attempt=2)→attempts≥MaxRetry→DLQ 整条 worker 路径从未被 broker 实测。真实日志：`已投入重试桶 attempt=1/2 bucket=1s → 重试耗尽进 DLQ error=boom → 消费者组退出`，2.11s PASS。s2-4 累计修复 12 处、E2E 2 条，异步层全路径验证完毕

**s2-4 五轮（集成性终检，审查闭环）**：通读四文件清理残留——⑬ NewRabbitEnqueuer 对 nil Registry 兜底（此前 EnsureTopology 会 panic）；⑭ 移除 publish 恒 0 死参数 retryCount（retry 计数归 consumer 失败路径，防误导）。E2E `-count=2` 连跑 7.2s 双 PASS 无 flake。**s2-4 审查正式闭环（累计 14 处修复）**——异步层交付达标，不再加轮；后续走 s2-5 阶段整体自审

**s2-4 六轮（用户质疑"问题太多"，答复性验证）**：对 14 处做严重度分类澄清——真正运行级/资金级约 5 处（无 confirm 的假确认、mainExchange 错位、在途中断、索引/参数等其余为防御性加固与 API 假设修正，编译器不可见）。终态通读 + `go test -count=1 ./...`（全包）+ E2E 非缓存重跑 + vet 全绿，**本轮零新增缺陷**。s2-4 定稿，停止加轮

## 阶段 3 推进（角色启动链路，B1 验收 healthz 打通）

- `task.EnsureBrokerTopology` 便捷导出（拨号+幂等声明+关闭，任意角色启动前用）
- `internal/httpserver`：gin 骨架（新增 gin 依赖），/healthz（无依赖）+ /readyz（PG Ping 2s 超时，强依赖，09 §7）
- `cmd/cloudfog` 重写：`--role=api|worker|scheduler|all`（另有 migrate/version）；启动链路 = dotenv → config.Load 全量校验 → slog logger → DB Open+Ping → `migrate.RequireSchemaVersion(20260906000001)` 门禁 → 角色 goroutine → signal(SIGINT/SIGTERM) 优雅关闭（API Shutdown 用 cfg.Server.ShutdownTimeout）
- worker 角色：EnsureBrokerTopology + taskHandlers（当前空→只声明拓扑不消费，防误 DLQ）；config→task 映射 toConsumer；scheduler 基座占位（负载待接线）
- **真实冒烟**：`--role=all` 进程 `/healthz 200` `/readyz 200`（PG ping 通），进程稳定 4s 无错；B1 验收核心达成
- 后续（s3-2）：bootstrap 种子（需 repository）、scheduler 任务接线、handler 注册进服务、Rabbit WAL 降级、CI/testcontainers 补 rabbit+集成跑测

**s3-2 完成：RabbitMQ 降级 WAL（internal/task/fallback.go）**：`ResilientEnqueuer` 包装 primary——broker 可用直通（confirm 语义不降级）；不可用 → 任务落 **JSONL WAL（append+fsync，唯一持久事实源）** 且返回成功不丢；broker 恢复（任一次成功投递触发）异步单飞重放，**成功条才移除（临时文件+rename 原子重写）**——任意时刻崩溃最多重复、不丢失（业务幂等键兜底）。EnqueueIn 降级期为尽力而为（重放即时），有注释说明。修复：dispatch 先按延迟分支（曾 bug：EnqueueIn 先试 Enqueue 成功即丢延迟）；recover 直接走 primary 避免递归/重复落盘。单测 3 条：降级→恢复重放收敛+WAL 清空、**模拟重启后既有 WAL 重放**、broker 正常时不落盘且延迟语义保留。全量 BUILD/VET/TEST/gofmt 绿。**接线待产端出现时**：cmd wiring 将 RabbitEnqueuer 包一层 NewResilient(walPath e.g. data/wal/tasks.wal)。s3-2 剩余依赖 repository/domain（bootstrap/scheduler/handler）→ 阶段 4

**WAL 模块审查修复（1 数据丢失缺陷）**：recover 快照重放期间并发 appendWAL 的新条目会被结尾原子重写**整段截掉**（丢失窗口）。修复：**recover 全程持 r.mu**（禁止恢复期间并发落盘；恢复期新降级投递短暂阻塞，正确性>吞吐）。新增回归 `TestResilient_NoLossDuringConcurrentDegradeAndReplay`（slow primary 拉宽窗口 + flap 注入 C 并发投递，断言 A/B/C/D 全达且 WAL 清空；修复前 delivered=3 会失败）。同步修正延迟语义注释（恢复期原 delay 重走 EnqueueIn）。全绿

## S3 整体审计（集成视角，1 可用性修复 + 测试补白）

- **修复**：`--role=all` dev 场景 worker `EnsureBrokerTopology` 连不上 RabbitMQ 会经 runErr 连带杀死 API——B1（handler 空）下改为告警+idle（拓扑缺失无消费影响）；handler 已接线时仍 fail-fast（真部署语义不变）
- **测试补白**：httpserver 新增单测（/healthz、db=nil 时 /readyz 200、未知路径 404）
- **复核通过项**：role 白名单快速失败；关闭序（API Shutdown→waitRoles）；schema 门禁先于角色；runErr buffered 无泄漏；toConsumer 映射与 config↔task 解耦；`--config` 双源 + env 别名有单测
- **登记备注**：config.Validate 全局强制 rabbit/redis 必填（api-only 也需这些 env——fail-fast 一致，接受）；MasterKeyID 无 env 别名（默认 k_2026_08，轮换时走 YAML）；worker 无 handler 时与 broker 全断连语义=待机不重连（supervisor 阶段 4 引入）；main/httpserver/cmd 无更多自动化但关键路径已有单测 + 冒烟
- 终态：BUILD/VET/TEST/gofmt 全绿

**S3 二轮审计（broker 宕机实证）**：停 RabbitMQ → `--role=all` 进程存活 True、healthz 200、日志 `WARN worker：RabbitMQ 不可达且业务 handler 未接线，worker 待机（...dial tcp ... refused）`——上轮修复得到真实故障场景验证，API 不被连带杀死。二轮零新增代码缺陷（免循环），S3 审查闭环

## S1-S3 联动审计（接缝一致性，1 处修复）

- 综合实证全绿：config 默认 exchange 与 task.DefaultExchange 同值 `cloudfog.tasks`；migrate up CLI 与文档 compose migrate 服务匹配；全模块 `-count=1` 测试过；task 集成 E2E（rabbit 恢复）过；DB `schema_migrations=20260906000001/false`、36 表；vet/gofmt 干净
- **修复**：Makefile `test-unit` 用了不存在的 `unit` build tag（代码无任何 unit tag；仅 smoke_test.go 带 `integration`）——改为 `go test -count=1 ./...`；`test-integration` 显式 `-tags=integration`；`test-race` 去掉 unit tag
- 工程件齐整：Makefile（dev=compose-up→migrate up→--role=all）、.golangci.yml、.env.example/.gitignore；无临时残留（exe/out/tmp/wal/log 均为空）
- S1/S2/S3 交付物与各自审查记录齐备，阶段 3 收尾

## s3-2 全部待办完成（最后一项 todo 闭环，B1 收尾）

- **repository**（internal/repository）：GORM 数据访问最小集（User 查询/创建 + UpsertBuiltinProvider/Model 幂等种子——仅覆盖 source='builtin'，运营/迁移数据不覆盖，02 §16/01 §10.4）。**Type 陷阱**：model.Source 为匿名嵌入结构，访问需 `cur.Source.Source`（不能用 `cur.Source` 比字符串）
- **password**（internal/pkg/password）：argon2id（PHC 自描述，v=19/m=64MiB/t=1/p=4/k=32）+ 常量时间比对；bcrypt/md5_legacy 显式拒绝直连（迁移走 13-operations §5.3）；单测
- **bootstrap**（internal/bootstrap + cmd `bootstrap` 子命令 + `--role=all` 自动引导）：事务内建超管（argon2id + super_admin/active）+ 内置 provider(openai/anthropic) + model(gpt-4o/claude-sonnet-4)；**真实 PG 验证：两次运行幂等（super_admin=1、builtin 种子各 2、total_users=1）**
- **scheduler 接线**（cmd/scheduler.go）：进程内 cron 引擎（robfig/cron v3），spec 来自 config.Scheduler，逐任务注册；payload 由 `periodicBuilders` 注册表构造（B1 空 → 到点仅 debug 不投递）；投递走**懒加载 Resilient(RabbitMQ,WAL)**——broker 未起不影响 scheduler 启动（保持前序 broker 宕机韧性实证）
- **handler 注册表**（task/handlers.go）：`RegisterHandler/Handlers/HandlerCount` 跨包注入（重复注册防护），worker 启动用快照；B1 空 → worker 不消费（语义不变）
- WAL 已于前述轮次完成。终态：BUILD/VET/TEST 全绿、bootstrap 真实验证幂等

**最后一项 todo 再审修复（1 永久性故障）**：scheduler 懒加载投递器原用 `sync.Once`——首次连接失败（broker 短暂不可用/晚启）会把 enqErr 永久记住，broker 恢复后周期任务永远无法投递（builder 空时不可见，接线后必踩）。改双检锁 + **失败可重试**（每次到点重试连接；成功才缓存实例）。备注（接受）：ensureSuperAdmin 并发重跑存在唯一键竞争窗口（单写者场景，dev/一次性子命令可接受）；超管 Username=Email 受 varchar(64) 限制（超长邮箱会报错而非截断——错误可见可接受）。全绿

**最后一项 todo 二轮再审（安全硬化 1 处）**：`verifyArgon2` 对 PHC 参数不加约束——被注入巨额 m/t 的恶意哈希（如 m=10GB/t=9999）会让登录比对触发内存/CPU 耗尽 DoS。补上限防护（m≤512MiB、t≤16、p≤16 且 p>0，超限在**分配前拒绝**）+ 恶意哈希单测。三测试 PASS。接受性备注维持：bootstrap 并发唯一键窗口、Username 长度

**最后一项 todo 三轮再审（终审，0 新增）**：三张注册表完备性核对——task 常量 15 = registry 绑定 15（全类型有队列归属）；scheduler specs 11（调度驱动子集，全部已绑定）。`--role=all` 端到端实证：存活、healthz 200、日志 `bootstrap 完成 admin@cloudfog.local` + `scheduler 已启动 wired_builders=0 jobs=11`（cron 引擎 11 job 就绪、builder 空不投递）、第 4 次自动引导后 `admin_again=1`（幂等）。零新增缺陷，**审查正式关闭（3 轮：sync.Once 永久失败→argon2 DoS→完备性实证）**，不再就同范围加轮

## B1 收官全面审计（仓库普查 + 验收门禁全链路）

- 仓库：41 src + 10 test 文件；源码 3375 行 + 测试 1246 行 + 迁移 SQL 532 行；无 TODO/FIXME（仅 Registry 防误配预期 panic 与测试内敏感样例串）；无临时残留
- 验收门禁全过：① 全模块单测 -count=1 绿；② task E2E（4.2s）绿；③ 迁移 down1→up 幂等（applied/36 表）；④ `--role=all` 存活 + /healthz 200 + /readyz 200 + 自动引导（重建后 admin=1/providers=2/models=2）
- **B1 验收对照**：make dev 链路 ✓（等价手工验证）；/healthz ✓；PG 20+ 表 ✓（36）；门禁本地全绿 ✓
- **未完成/边界清单（下阶段）**：CI 流水线未建（GitHub Actions+testcontainers 候选）；race 检测需 Linux gcc；Redis 连接尚未使用（auth/cache/限流=B2）；handler/periodic builder 空=按设计（等 B2 服务）；repository 为最小集；登录/鉴权/API 业务=B2；http 超时未接 config；验证均在 Windows+本机容器

## CI 门禁化完成（.github/workflows/ci.yml 重写）

- **lint** job：gofmt -l 空检查 + golangci-lint + go vet
- **test** job：GitHub services 起真实 **postgres:18-alpine + rabbitmq:4.2-management**（生产同 tag）；步骤=单元(含 -race，移除旧 `-tags=unit`) → **迁移幂等门禁 up→down 1→up** → **bootstrap 两轮幂等** → 异步层 E2E（-tags=integration）
- **security**：gitleaks + govulncheck；**build**：静态二进制 + artifact 上传（needs lint+test）
- 仓库根 project/（git root 在上级）；workflow 放 api/.github/workflows/ci.yml（git 根为 project，trigger 需仓库推送路径匹配——若 CI 未触发需把 workflow 上移一级或在 repo settings 启用 api 子路径）
- 本地无法运行 GitHub Actions；YAML 人工复核（块标量/嵌套/缩进规范）；push 后首次运行验证

**阶段3 首轮审查修复（2 处）**：① 非法 `--role=typo` 此前全部角色 flag=false 会 `select` 无限挂起无报错——补 role 白名单（api|worker|scheduler|all）快速失败；② 角色 goroutine 不被等待——ctx.Done 后 worker drain 可能被进程退出截断——补 roleWG + 先 Shutdown API 再 waitRoles(timeout) 的关闭序（01 §7.3 优雅语义）。其余备注：http server 尚未应用 config 的 Read/Idle 超时（B1 骨架可接受）；gin 请求日志后续随 handler/metrics 层挂 slog。

**s3-2 首批完成（配置便捷项）**：① cmd 支持 `--config=<path>`（config.yaml 双源，10 §3.1）；② flatAliases 补 observability.log_level/log_format → CLOUDFOG_LOG_LEVEL/CLOUDFOG_LOG_FORMAT + 单测。**剩余 s3-2 依赖判定**：bootstrap 种子（超管/内置 provider 需 repository + 密码哈希策略）、scheduler 周期任务接线（需领域 payload 构造 + handler）、handler 注册（需 service 层）均属**下一里程碑（阶段 4 repository/domain 起步）**的前置——为避免"无业务代码硬造种子"的伪进度，建议转入阶段 4 先建 repository 与 domain；RabbitMQ 降级 WAL（TaskEnqueuer failover + 落盘重放）为自包含模块，可作为独立先行项。
**s2-2 审查修复（4 处）**：① users.phone 可空（*string，02 §3.1"可空，唯一"）；② api_keys.group_id 去掉多余裸 index（与 §3.3 索引清单漂移）；③ user_allowed_groups 去掉多余 created_at（与 channel_groups 同为纯关联表）；④ usage_log 还原 §15.4 canonical（upstream_model/mapping_chain/cache_write_ttl/error_code/client_ip/user_agent 移除擅自加的 not null default ''）。
**遗留备注（s2-3 处理）**：a) channel.source_id canonical 用非空 string 而 §16 说"两列均可空"——迁移建表统一 nullable；b) usage_daily_stats 唯一键含可空列 → PG18 用 UNIQUE NULLS NOT DISTINCT（或 COALESCE 哨兵）；c) email/username 部分唯一、uq_ledger_settle/refund、uq_subscription_period、order 部分唯一等全在迁移 SQL。

**s2-2 第二轮审查（纵深）**：
- 新增 `model_schema_test.go` 解析期守卫：用 gorm/schema.Parse 过全部 22 模型，断言 TableName 与主键存在——**抓到编译器不可见的真 bug**：Decimal 字段 tag `default:1.0` 会让 GORM 在 schema 解析期报错（内部按 bool 走 ParseBool 失败）。已把 channel/group/subscription_plan/payment_order 的 `default:1.0` 归一为 `default:1`（DB 真实默认值仍由迁移 SQL 声明）。测试现 PASS 且锁定防回归
- 结构性审计：25 go 文件（db/common/types+22）、22 个 TableName 与 doc 表名逐一精确对齐、uniqueIndex 全表仅 5 个（api_keys.key_hash/payment_orders.order_no/payment_providers.code/providers.code/subscription_plans.name），与 doc 全局唯一一致
- 语义备注：a) 5 个 full unique 列所在 4 表带 SoftDelete → 软删后同名码无法重建（doc 定义为全局唯一，符合 doc，运维留意）；b) 索引方向 DESC 是迁移 SQL 的归属（GORM tag 只能声明列序不能声明方向）；c) doc 部分表字段清单省略 created/updated/deleted 标准列，统一按 §15.2 业务实体嵌入策略落地（含 Source 溯源）
- 终验 BUILD/VET/TEST/gofmt 全绿；s2-2 无遗留阻塞
- 进行中：s2-3 migrations/0001_init 22 核心表建表 SQL（.up.sql/.down.sql，分区 usage_logs、复合主键、部分唯一 uq_ledger_settle/refund、uq_subscription_period、CHECK balance>=0 等）

## 全量代码审查（2026-09-06 下午，不含文档；11 文件全读）

- **发现并修复安全缺陷**：logger.redactAttr 此前只脱敏顶层 attr，不递归 slog.Group 子键——`slog.Group("billing", slog.String("credentials","明文"))` 可绕过检测直接落盘（违反 08 §4.3）。修复：Group 递归脱敏（保留组结构与无关字段）+ 回归测试 Test_GroupChild_Redacted。测试证明修复前必败、修复后组内字段正确
- **db.go 清理**：移除误导性 no-op `DisableAutomaticPing:false`，注释改为明确 AutoMigrate 纪律靠约定+测试（12 §5.1）
- **低风险备注（未改）**：① `req` 命名反直觉（cond=true 才报错）；② email.enabled 默认 true 但未校验 smtp_host 非空（fail-fast 不完整）；③ `model.Open` 无单测（GORM 直连路径尚未被任何测试执行——待 testcontainers 集成补）；④ viper env 优先级 > config，YAML 中为必填键写 ${VAR} 实为无效写法（代码注释已说明）
- 终验：GOFMT CLEAN / BUILD 0 / VET 0 / TEST 0
- **复审（第二轮纵深）**：`go test -count=1` 全量非缓存通过；`-shuffle=on` 通过（无 env 污染/顺序依赖）；`go mod verify` all modules verified；**race 检测不可用**（本机无 gcc，需 cgo，登记为工具链限制）；staticcheck/golangci-lint 均未安装（go vet 为基线）。未发现新缺陷，上轮安全修复保持有效
